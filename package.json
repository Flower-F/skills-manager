{
  "name": "skills-manager",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "typecheck": "node --check skills/skills-manager/scripts/customization-patch.mjs && node --check tests/*.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
