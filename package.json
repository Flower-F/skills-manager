{
  "name": "skills-manager",
  "version": "0.1.0",
  "description": "Agent guidance for npx skills with semantic customization Intents.",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "typecheck": "node --check skills/skills-manager/scripts/customization-patch.mjs && node --check scripts/*.mjs && node --check tests/*.mjs",
    "check:distribution": "node --test tests/distribution.test.mjs",
    "check:dco": "node scripts/check-dco.mjs",
    "check:history": "node scripts/verify-history.mjs",
    "smoke:clean": "node scripts/smoke-clean-checkout.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
