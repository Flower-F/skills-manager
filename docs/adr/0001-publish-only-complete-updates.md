# Publish only complete skill updates

An installed skill remains on its previous validated rendering while an upstream update is fetched, customized, validated, and reviewed in staging. The staged rendering replaces the published skill only after the complete managed update is accepted, so a failed or paused update never exposes an uncustomized or partially applied skill.
